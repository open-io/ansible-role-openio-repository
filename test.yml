# Test playbook for Ansible role cdelgehier.openio-repository
---

- hosts: all
  gather_facts: true
  become: true
  roles:
    - role: role_under_test
    - role: role_under_test
      openio_repository_release: "16.10"
      when: ansible_distribution != 'Debian'
    - role: role_under_test
      openio_repository_release: "17.04"
      openio_repository_product: test
      openio_repository_user: foo
      openio_repository_password: bar
